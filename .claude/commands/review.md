---
description: Review flashcards and drills that are due
argument-hint: [node or module — omit for everything due today]
---

Run a practice review using the `drill` skill.

Scope: $ARGUMENTS

If no scope was given, review everything due today across `learner_records/deck.md`.

Grade each card Again / Hard / Good / Easy, update intervals per SM-2, and write the results back to `deck.md`. Flag any card graded Again three times running — that is a node problem, not a card problem, and it belongs back in the teaching loop.

Finish by reporting what is due next, and when.
