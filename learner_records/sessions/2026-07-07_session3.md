# Session Log: 2026-07-07_session3

## Summary
Milestone session. The learner derived the complete, minimal, overlapping `1-0-1` sequence detector FSM on paper — every state and all 8 transitions — through trap-sequence reasoning. Milestone 3 (State Transition Diagrams) core concept is mastered; only the Logisim implementation remains.

## Key Activities
1. **Active Recall (passed):** AND gate = 0 states, no memory — last session's misconception did not recur.
2. **State Enumeration:** Learner proposed 4 states for the `1-0-1` detector. A labeling ambiguity ("state 0" = start vs. "seen a lone 0") triggered a productive detour through **state equivalence** — no future input distinguishes "power-on" from "seen only 0s," so they are one state. Adopted clean labels S0–S3 to avoid state-label/input collision.
3. **Trap-Sequence Analysis:**
   - `1001`: learner feared false fire; trace proved the S2 --0--> S0 arrow destroys dead progress (`0,0` starts nothing). Lesson: safety lives in transitions, not extra states.
   - `10101`: learner first answered S3 --0--> S0 (non-overlapping — misses the second occurrence); corrected to S3 --0--> S2 after tracing. Lesson: don't discard live progress; **transitions encode reuse, states encode progress**.
   - `11` (S1 self-loop): learner correctly reasoned the new `1` replaces the old as live progress.
4. **Final Deliverable:** Complete transition table, verified 4 states × 2 inputs = 8 arrows, no gaps:
   | State | Meaning | on 0 | on 1 |
   |---|---|---|---|
   | S0 | no progress | S0 | S1 |
   | S1 | seen 1 | S2 | S1 |
   | S2 | seen 1,0 | S0 | S3 🔔 |
   | S3 | fired | S2 | S1 |

## Decisions Made
- Detector spec: **overlapping** (S3 --0--> S2).
- Milestone 3 marked *concept mastered* in roadmap; Logisim implementation moved under Module 2 execution.
- Implementation path fixed: 2-bit state encoding (S0=00, S1=01, S2=10, S3=11) → two D flip-flops → next-state truth table → K-map simplification → Logisim.

## Misconception Notes
- Initial instinct on S3 --0--> was S0 (discarding live progress) — the mirror image of the `1001` error. Resolved via trace; re-test lightly next session.
- State labels that look like input values ("state 0") cause confusion — convention S0/S1/... adopted.

## Unresolved Gaps / Homework
- **Read Harris & Harris Chapters 1–2** (Milestone 1, now blocking): K-maps are required to simplify the detector's next-state logic.
- Optional head start: write the 8-row next-state truth table (state bits Q1 Q0 + input → next Q1' Q0' + fire output) from the transition table.
- Milestones 1 & 2 (reading, 4-bit adder lab) still not started.

## Next Session Entry Point
1. Light active recall: "Why does S3 --0--> go to S2 and not S0?"
2. Collect homework: truth table from the transition table.
3. Begin state encoding + K-map simplification, then Logisim wiring (Module 2 goal).
